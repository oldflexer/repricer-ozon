# BEGIN_UPDATE_PRICE_TIMER_{{INSTANCE_NAME}}
# Обновление таймера актуальности минимальной цены (каждый день в 05:00)
0 5 * * * cd {{WORKING_DIR}} && {{WORKING_DIR}}/.venv/bin/python scripts/update_price_timer.py --all >> {{WORKING_DIR}}/logs/update_price_timer-{{INSTANCE_NAME}}.log 2>&1
# END_UPDATE_PRICE_TIMER_{{INSTANCE_NAME}}